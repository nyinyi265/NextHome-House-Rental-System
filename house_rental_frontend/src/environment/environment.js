// central environment configuration for frontend

// You can replace the hardcoded value here or set it dynamically via
// window.__ENV__ (injected by server) or any other mechanism.

export const API_BASE_URL = 'http://127.0.0.1:8000/api';